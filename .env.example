# MongoDB Connection
URI=mongodb://localhost:27017/test

# Server
PORT=4000
FRONTEND_URL=http://localhost:3000

# Admin
ADMINNAME=admin
PASSWORD=Admin@567
hash=123edc

# Firebase Admin (for candidate portal auth)
# Required for /api/candidate/* endpoints. Get from Firebase Console → Project Settings → Service Accounts
FIREBASE_PROJECT_ID=adamanthr-b6f7c

# Option A: Path to service account JSON (download from Firebase Console)
# FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json

# Option B: Or set these from the JSON:
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@adamanthr-b6f7c.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Internal Admin Auth (Firebase email/password + domain allowlist)
# Only these domains can access hr-admin (enforced in backend verifyAdmin)
INTERNAL_EMAIL_DOMAINS=yourdomain.com
# Bootstrap superadmins (comma-separated full emails) - optional but recommended for first setup
INTERNAL_SUPERADMINS=superadmin@yourdomain.com

# Email OTP (candidate portal sign-in via email code)
# Required for POST /api/candidate/send-email-otp
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-user
# SMTP_PASS=your-smtp-password
# MAIL_FROM=Career Portal <noreply@example.com>
