# MongoDB Connection
URI=mongodb://localhost:27017/test

# Server
PORT=4000
FRONTEND_URL=http://localhost:3000

# Admin
ADMINNAME=admin
PASSWORD=Admin@567
hash=123edc

# Firebase Admin (for candidate portal auth)
# Required for /api/candidate/* endpoints. Get from Firebase Console → Project Settings → Service Accounts
FIREBASE_PROJECT_ID=adamanthr-b6f7c
# Storage bucket for resume downloads. Must match portal's firebaseConfig.storageBucket (e.g. {project}.firebasestorage.app)
FIREBASE_STORAGE_BUCKET=adamanthr-b6f7c.firebasestorage.app

# Option A: Path to service account JSON (download from Firebase Console)
# FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json

# Option B: Or set these from the JSON:
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@adamanthr-b6f7c.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Internal Admin Auth (Firebase email/password + domain allowlist)
# Only these domains can access hr-admin (enforced in backend verifyAdmin)
INTERNAL_EMAIL_DOMAINS=yourdomain.com
# Bootstrap superadmins (comma-separated full emails) - optional but recommended for first setup
INTERNAL_SUPERADMINS=superadmin@yourdomain.com
# Primary super admin email for document-verification notifications (if unset, first from INTERNAL_SUPERADMINS is used)
# PRIMARY_SUPERADMIN_EMAIL=superadmin@yourdomain.com

# Phone region: US (dev) or IN (India). Affects validation (US: 11 digits starting 1; IN: 12 digits starting 91).
# PHONE_REGION=US

# Email OTP (candidate portal sign-in via email code)
# Required for POST /api/candidate/send-email-otp (SendGrid Web API)
# SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxx
# SENDGRID_MAIL_FROM=Career Portal <noreply@yourdomain.com>

# SMS OTP (phone merge on Complete Profile – “Send code” for phone conflict)
# Required for POST /api/candidate/send-merge-phone-otp (Twilio)
# TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# TWILIO_AUTH_TOKEN=your_auth_token
# TWILIO_PHONE_NUMBER=+1234567890

# Job Alerts (uses same SendGrid as email OTP)
# CANDIDATE_PORTAL_URL=https://careers.yourdomain.com
